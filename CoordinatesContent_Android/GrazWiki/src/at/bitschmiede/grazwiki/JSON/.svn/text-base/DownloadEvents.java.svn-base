package at.bitschmiede.grazwiki.JSON;

public interface DownloadEvents {
	void downloadBuildingsOfRegionCompleted(Building[] buildingsInRegion);
	void downloadAllBuildingsCompleted(Building[] buildingsInRegion);
	void downloadAllRoutesCompleted(Route[] routes);
	void downloadRouteInformationCompleted(int route_id, RouteInformation routeInformation);
	void downloadBuildingInformationCompleted(int buildingID, BuildingDescription[] buildingDescriptions);
	void downloadAllCategoriesCompleted(Category[] categories);
	void downloadBuildingsOfCategoryCompleted(Building[] buildingsOfCategory);
}
